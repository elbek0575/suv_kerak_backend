янги SECRET_KEY тузиш ккомандаси:
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

Запуск ngrok
D:\Install\ngrok\ngrok.exe http 8000

Деплой килмаган вактда шу командани териш керак. HEROKU_API_KEY бекор килинади ва логин оркали деплой булади.
Remove-Item Env:HEROKU_API_KEY
heroku login

git add .
git commit -m " янги агент апп қўшилди"
git push heroku main


git add .
git branch -M main
git push -u origin main

heroku run python manage.py collectstatic -a ecohisob-backend

python manage.py runserver

Миграция килиш
python manage.py makemigrations
python manage.py migrate



pip freeze > requirements.txt


Remove-Item Env:HEROKU_API_KEY
heroku login
heroku repo:purge_cache -a ecohisob-backend
git push heroku main





⚡ Хулоса:

list_display = қайси устунлар руйхатда чиқиши.

search_fields = қайси устунларда қидириш.

list_filter = қайси устунлар бўйича фильтр.

readonly_fields = формада кўрилади, лекин таҳрирланмайди.